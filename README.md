# lp-empresas

Landing page B2B — https://empresas.maestrosdaia.com/

Static single-file site. Source of truth: `index.html` (deploy = repo root, no build step). Netlify git-backed auto-deploy on push to `main`.
